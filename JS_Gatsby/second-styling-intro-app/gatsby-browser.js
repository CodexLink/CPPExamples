// ! This is how we define our CSS.

/*
    ! In React, we prefer ES6 Styling Implementations.
    ! Therefore, In React App with Prefer ES6.
    ! While in NodeJS, we do prefer the CommonJS which is
    * the const requiredFile = require('filepath');

    ! Both Instances Are Working. Just Preferences Though.
*/

import "./src/styles/globals.css";